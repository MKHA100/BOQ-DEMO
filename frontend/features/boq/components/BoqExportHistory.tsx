import type { BoqExport } from "../types";

export function BoqExportHistory({ exports, onDownload }: { exports: BoqExport[]; onDownload: (item: BoqExport) => void }) {
  return <section className="overflow-hidden rounded-2xl border border-slate-200 bg-white"><div className="border-b border-slate-200 px-5 py-4"><h3 className="font-semibold">Export history</h3><p className="mt-1 text-sm text-slate-500">Files are cached by quantity, template, setup, format, and floor selection.</p></div><div className="overflow-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-5 py-3">File</th><th className="px-5 py-3">Layout</th><th className="px-5 py-3">Versions</th><th className="px-5 py-3">Created</th><th className="px-5 py-3">Status</th><th className="px-5 py-3" /></tr></thead><tbody>{exports.map((item) => <tr key={item.id} className="border-t border-slate-200"><td className="px-5 py-3 font-semibold">{item.filename}</td><td className="px-5 py-3 capitalize">{item.floor_mode.replaceAll("_", " ")}</td><td className="px-5 py-3 text-xs text-slate-500">BOQ {item.boq_version} · Template {item.template_version} · Setup {item.setup_version || 1}</td><td className="px-5 py-3">{new Date(item.created_at).toLocaleString()}</td><td className="px-5 py-3"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${item.status === "ready" ? "bg-emerald-50 text-emerald-700" : item.status === "failed" ? "bg-red-50 text-red-700" : "bg-blue-50 text-blue-700"}`}>{item.status}</span>{item.error_message ? <p className="mt-1 max-w-72 text-xs text-red-600">{item.error_message}</p> : null}</td><td className="px-5 py-3 text-right">{item.status === "ready" ? <button className="font-semibold text-blue-600" onClick={() => onDownload(item)}>Download</button> : null}</td></tr>)}</tbody></table>{!exports.length ? <div className="p-12 text-center text-sm text-slate-500">No exports created yet.</div> : null}</div></section>;
}
